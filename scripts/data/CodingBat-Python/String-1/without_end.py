"""
CodingBat > Python > String-1 > withoutEnd

Given a string, return a version without the first and last char, so "Hello"
yields "ell". The string length will be at least 2.

Examples:
  withoutEnd("Hello") → "ell"
  withoutEnd("java") → "av"
  withoutEnd("coding") → "odin"
  withoutEnd("code") → "od"
  withoutEnd("ab") → ""
  withoutEnd("Chocolate!") → "hocolate"
  withoutEnd("kitten") → "itte"
  withoutEnd("woohoo") → "ooho"
"""


def without_end(str):
    # TODO: implement
    return ""
