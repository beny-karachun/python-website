"""
CodingBat > Python > String-1 > theEnd

Given a string, return a string length 1 from its front, unlessfrontis false,
in which case return a string length 1 from its back. The string will be
non-empty.

Examples:
  theEnd("Hello", True) → "H"
  theEnd("Hello", False) → "o"
  theEnd("oh", True) → "o"
  theEnd("oh", False) → "h"
  theEnd("x", True) → "x"
  theEnd("x", False) → "x"
  theEnd("java", True) → "j"
  theEnd("chocolate", False) → "e"
  theEnd("1234", True) → "1"
  theEnd("code", False) → "e"
"""


def the_end(str, b):
    # TODO: implement
    return ""
