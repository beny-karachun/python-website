"""
CodingBat > Python > String-1 > withoutX

Given a string, if the first or last chars are 'x', return the string without
those 'x' chars, and otherwise return the string unchanged.

Examples:
  withoutX("xHix") → "Hi"
  withoutX("xHi") → "Hi"
  withoutX("Hxix") → "Hxi"
  withoutX("Hi") → "Hi"
  withoutX("xxHi") → "xHi"
  withoutX("Hix") → "Hi"
  withoutX("xaxbx") → "axb"
  withoutX("xx") → ""
  withoutX("x") → ""
  withoutX("") → ""
  withoutX("Hello") → "Hello"
  withoutX("Hexllo") → "Hexllo"
"""


def without_x(str):
    # TODO: implement
    return ""
