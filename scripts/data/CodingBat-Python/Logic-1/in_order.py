"""
CodingBat > Python > Logic-1 > inOrder

Given three ints, a b c, return true if b is greater than a, and c is greater
than b. However, with the exception that if "bOk" is true, b does not need to
be greater than a.

Examples:
  inOrder(1, 2, 4, False) → True
  inOrder(1, 2, 1, False) → False
  inOrder(1, 1, 2, True) → True
  inOrder(3, 2, 4, False) → False
  inOrder(2, 3, 4, False) → True
  inOrder(3, 2, 4, True) → True
  inOrder(4, 2, 2, True) → False
  inOrder(4, 5, 2, True) → False
  inOrder(2, 4, 6, True) → True
  inOrder(7, 9, 10, False) → True
  inOrder(7, 5, 6, True) → True
  inOrder(7, 5, 4, True) → False
"""


def in_order(n, n2, n3, b):
    # TODO: implement
    return False
