/**
 * CodingBat > Java > String-2 > bobThere
 * https://codingbat.com/prob/p175762
 *
 * Return true if the given string contains a "bob" string, but where the middle
 * 'o' char can be any char.
 *
 * Examples:
 *   bobThere("abcbob") → true
 *   bobThere("b9b") → true
 *   bobThere("bac") → false
 *   bobThere("bbb") → true
 *   bobThere("abcdefb") → false
 *   bobThere("123abcbcdbabxyz") → true
 *   bobThere("b12") → false
 *   bobThere("b1b") → true
 *   bobThere("b12b1b") → true
 *   bobThere("bbc") → false
 *   bobThere("bbb") → true
 *   bobThere("bb") → false
 *   bobThere("b") → false
 */
public class BobThere {

    public boolean bobThere(String str) {
        // TODO: implement
        return false;
    }

}
