/**
 * CodingBat > Java > Recursion-1 > strDist
 * https://codingbat.com/prob/p195413
 *
 * Given a string and a non-empty substringsub, compute recursively the largest
 * substring which starts and ends with sub and return its length.
 *
 * Examples:
 *   strDist("catcowcat", "cat") → 9
 *   strDist("catcowcat", "cow") → 3
 *   strDist("cccatcowcatxx", "cat") → 9
 *   strDist("abccatcowcatcatxyz", "cat") → 12
 *   strDist("xyx", "x") → 3
 *   strDist("xyx", "y") → 1
 *   strDist("xyx", "z") → 0
 *   strDist("z", "z") → 1
 *   strDist("x", "z") → 0
 *   strDist("", "z") → 0
 *   strDist("hiHellohihihi", "hi") → 13
 *   strDist("hiHellohihihi", "hih") → 5
 *   strDist("hiHellohihihi", "o") → 1
 *   strDist("hiHellohihihi", "ll") → 2
 */
public class StrDist {

    public int strDist(String str, String str2) {
        // TODO: implement
        return 0;
    }

}
