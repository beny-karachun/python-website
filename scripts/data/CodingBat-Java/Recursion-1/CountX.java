/**
 * CodingBat > Java > Recursion-1 > countX
 * https://codingbat.com/prob/p170371
 *
 * Given a string, compute recursively (no loops) the number of lowercase 'x'
 * chars in the string.
 *
 * Examples:
 *   countX("xxhixx") → 4
 *   countX("xhixhix") → 3
 *   countX("hi") → 0
 *   countX("h") → 0
 *   countX("x") → 1
 *   countX("") → 0
 *   countX("hihi") → 0
 *   countX("hiAAhi12hi") → 0
 */
public class CountX {

    public int countX(String str) {
        // TODO: implement
        return 0;
    }

}
