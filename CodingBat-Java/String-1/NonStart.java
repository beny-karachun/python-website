/**
 * CodingBat > Java > String-1 > nonStart
 * https://codingbat.com/prob/p143825
 *
 * Given 2 strings, return their concatenation, except omit the first char of
 * each. The strings will be at least length 1.
 *
 * Examples:
 *   nonStart("Hello", "There") → "ellohere"
 *   nonStart("java", "code") → "avaode"
 *   nonStart("shotl", "java") → "hotlava"
 *   nonStart("ab", "xy") → "by"
 *   nonStart("ab", "x") → "b"
 *   nonStart("x", "ac") → "c"
 *   nonStart("a", "x") → ""
 *   nonStart("kit", "kat") → "itat"
 *   nonStart("mart", "dart") → "artart"
 */
public class NonStart {

    public String nonStart(String str, String str2) {
        // TODO: implement
        return "";
    }

}
