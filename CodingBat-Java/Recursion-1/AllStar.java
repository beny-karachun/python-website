/**
 * CodingBat > Java > Recursion-1 > allStar
 * https://codingbat.com/prob/p183394
 *
 * Given a string, compute recursively a new string where all the adjacent chars
 * are now separated by a "*".
 *
 * Examples:
 *   allStar("hello") → "h*e*l*l*o"
 *   allStar("abc") → "a*b*c"
 *   allStar("ab") → "a*b"
 *   allStar("a") → "a"
 *   allStar("") → ""
 *   allStar("3.14") → "3*.*1*4"
 *   allStar("Chocolate") → "C*h*o*c*o*l*a*t*e"
 *   allStar("1234") → "1*2*3*4"
 */
public class AllStar {

    public String allStar(String str) {
        // TODO: implement
        return "";
    }

}
