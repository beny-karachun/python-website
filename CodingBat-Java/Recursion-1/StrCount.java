/**
 * CodingBat > Java > Recursion-1 > strCount
 * https://codingbat.com/prob/p186177
 *
 * Given a string and a non-empty substringsub, compute recursively the number
 * of times that sub appears in the string, without the sub strings overlapping.
 *
 * Examples:
 *   strCount("catcowcat", "cat") → 2
 *   strCount("catcowcat", "cow") → 1
 *   strCount("catcowcat", "dog") → 0
 *   strCount("cacatcowcat", "cat") → 2
 *   strCount("xyx", "x") → 2
 *   strCount("iiiijj", "i") → 4
 *   strCount("iiiijj", "ii") → 2
 *   strCount("iiiijj", "iii") → 1
 *   strCount("iiiijj", "j") → 2
 *   strCount("iiiijj", "jj") → 1
 *   strCount("aaabababab", "ab") → 4
 *   strCount("aaabababab", "aa") → 1
 *   strCount("aaabababab", "a") → 6
 *   strCount("aaabababab", "b") → 4
 */
public class StrCount {

    public int strCount(String str, String str2) {
        // TODO: implement
        return 0;
    }

}
