/**
 * CodingBat > Java > Recursion-1 > endX
 * https://codingbat.com/prob/p105722
 *
 * Given a string, compute recursively a new string where all the lowercase 'x'
 * chars have been moved to the end of the string.
 *
 * Examples:
 *   endX("xxre") → "rexx"
 *   endX("xxhixx") → "hixxxx"
 *   endX("xhixhix") → "hihixxx"
 *   endX("hiy") → "hiy"
 *   endX("h") → "h"
 *   endX("x") → "x"
 *   endX("xx") → "xx"
 *   endX("") → ""
 *   endX("bxx") → "bxx"
 *   endX("bxax") → "baxx"
 *   endX("axaxax") → "aaaxxx"
 *   endX("xxhxi") → "hixxx"
 */
public class EndX {

    public String endX(String str) {
        // TODO: implement
        return "";
    }

}
