"""
CodingBat > Python > String-1 > lastTwo

Given a string of any length, return a new string where the last 2 chars, if
present, are swapped, so "coding" yields "codign".

Examples:
  lastTwo("coding") → "codign"
  lastTwo("cat") → "cta"
  lastTwo("ab") → "ba"
  lastTwo("a") → "a"
  lastTwo("") → ""
"""


def last_two(str):
    # TODO: implement
    return ""
