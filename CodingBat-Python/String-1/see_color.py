"""
CodingBat > Python > String-1 > seeColor

Given a string, if the string begins with "red" or "blue" return that color
string, otherwise return the empty string.

Examples:
  seeColor("redxx") → "red"
  seeColor("xxred") → ""
  seeColor("blueTimes") → "blue"
  seeColor("NoColor") → ""
  seeColor("red") → "red"
  seeColor("re") → ""
  seeColor("blu") → ""
  seeColor("blue") → "blue"
  seeColor("a") → ""
  seeColor("") → ""
  seeColor("xyzred") → ""
"""


def see_color(str):
    # TODO: implement
    return ""
