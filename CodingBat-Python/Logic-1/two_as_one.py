"""
CodingBat > Python > Logic-1 > twoAsOne

Given three ints, a b c, return true if it is possible to add two of the ints
to get the third.

Examples:
  twoAsOne(1, 2, 3) → True
  twoAsOne(3, 1, 2) → True
  twoAsOne(3, 2, 2) → False
  twoAsOne(2, 3, 1) → True
  twoAsOne(5, 3, -2) → True
  twoAsOne(5, 3, -3) → False
  twoAsOne(2, 5, 3) → True
  twoAsOne(9, 5, 5) → False
  twoAsOne(9, 4, 5) → True
  twoAsOne(5, 4, 9) → True
  twoAsOne(3, 3, 0) → True
  twoAsOne(3, 3, 2) → False
"""


def two_as_one(n, n2, n3):
    # TODO: implement
    return False
