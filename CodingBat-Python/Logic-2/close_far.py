"""
CodingBat > Python > Logic-2 > closeFar

Given three ints, a b c, return true if one of b or c is "close" (differing
from a by at most 1), while the other is "far", differing from both other
values by 2 or more. Note: Math.abs(num) computes the absolute value of a
number.

Examples:
  closeFar(1, 2, 10) → True
  closeFar(1, 2, 3) → False
  closeFar(4, 1, 3) → True
  closeFar(4, 5, 3) → False
  closeFar(4, 3, 5) → False
  closeFar(-1, 10, 0) → True
  closeFar(0, -1, 10) → True
  closeFar(10, 10, 8) → True
  closeFar(10, 8, 9) → False
  closeFar(8, 9, 10) → False
  closeFar(8, 9, 7) → False
  closeFar(8, 6, 9) → True
"""


def close_far(n, n2, n3):
    # TODO: implement
    return False
