"""
CodingBat > Python > Recursion-1 > noX

Given a string, compute recursively a new string where all the 'x' chars have
been removed.

Examples:
  noX("xaxb") → "ab"
  noX("abc") → "abc"
  noX("xx") → ""
  noX("") → ""
  noX("axxbxx") → "ab"
  noX("Hellox") → "Hello"
"""


def no_x(str):
    # TODO: implement
    return ""
