"""
CodingBat > Python > Recursion-1 > endX

Given a string, compute recursively a new string where all the lowercase 'x'
chars have been moved to the end of the string.

Examples:
  endX("xxre") → "rexx"
  endX("xxhixx") → "hixxxx"
  endX("xhixhix") → "hihixxx"
  endX("hiy") → "hiy"
  endX("h") → "h"
  endX("x") → "x"
  endX("xx") → "xx"
  endX("") → ""
  endX("bxx") → "bxx"
  endX("bxax") → "baxx"
  endX("axaxax") → "aaaxxx"
  endX("xxhxi") → "hixxx"
"""


def end_x(str):
    # TODO: implement
    return ""
